# Workflows stack (n8n) — environment variables
# Copy to .env and fill in values (never commit .env)

N8N_ENCRYPTION_KEY=REPLACE_ME_RANDOM
N8N_USER_MANAGEMENT_JWT_SECRET=REPLACE_ME_RANDOM
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=REPLACE_ME
WEBHOOK_URL=http://hal-10k.local:5678/
